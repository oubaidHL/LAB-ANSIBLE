---
# Production environment variables
env: prod
dns_domain: iticparis.prod
nagios_server: nagios.iticparis.prod

# Environment-specific settings
deployment_mode: production
debug_enabled: false
log_level: warn

# Resource limits (prod uses full resources)
nginx_worker_processes: 4
nginx_worker_connections: 1024
